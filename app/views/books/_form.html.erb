<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :book_title %>
  </div>

  <div class="field">
    <%= form.label :author_first_name %>
    <%= form.text_field :author_first_name, id: :book_author_first_name %>
  </div>

  <div class="field">
    <%= form.label :author_last_name %>
    <%= form.text_field :author_last_name, id: :book_author_last_name %>
  </div>

  <div class="field">
    <%= form.label :year_published %>
    <%= form.number_field :year_published, id: :book_year_published %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
